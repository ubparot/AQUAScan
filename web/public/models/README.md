# AQUAScan Boat Model

Run `npm run convert:boat` from `web/` after installing Blender and adding `blender` to `PATH`.

The app looks for `public/models/speed-boat.glb`. If that file is missing, the Three.js scene uses a procedural boat fallback so the dashboard remains usable.
